---
import HeroApp from './react/Hero';
import type { LocaleCode } from '../data/i18n/locales';
import { defaultLocale } from '../data/i18n/locales';
import { productInfo, siteConfig } from '../data/site';
import { brandCopy } from '../data/site-core';
import { heroWidth, heroHeight, heroSrcSet, heroSizes, heroSrc } from '../lib/responsive-images';
import en from '../../public/locales/en/translation.json';

interface Props {
	locale?: LocaleCode;
}

const locale = Astro.props.locale ?? defaultLocale;
const monthly = productInfo.plans.find((p) => p.id === 'monthly') ?? productInfo.plans[0];
const useStaticHero = locale === 'en';

const chips = [
	{ key: 'chipEsp', label: brandCopy.chipEsp, icon: 'esp' },
	{ key: 'chipAim', label: brandCopy.chipAim, icon: 'aim' },
	{ key: 'chipRadar', label: brandCopy.chipRadar, icon: 'radar' },
	{ key: 'chipUpdates', label: brandCopy.chipUpdates, icon: 'updates' },
] as const;
---

{
	useStaticHero ? (
		<section class="hero" aria-label={siteConfig.name}>
			<div class="hero__media">
				<img
					src={heroSrc}
					srcset={heroSrcSet}
					sizes={heroSizes}
					alt={en.hero.imageAlt.replace('{{brand}}', siteConfig.name)}
					width={heroWidth}
					height={heroHeight}
					fetchpriority="high"
					loading="eager"
					decoding="async"
				/>
			</div>
			<div class="hero__veil" aria-hidden="true" />
			<div class="shell hero__content">
				<div class="hero__copy">
					<h1 class="hero__brand">
						<span data-edit="name">{siteConfig.name}</span>
					</h1>
					<p class="hero__lede" data-edit="heroLede">
						{brandCopy.heroLede}
					</p>
					<div class="hero__actions">
						<a class="hero__buy" href={siteConfig.checkoutUrl} rel="noopener noreferrer">
							<svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
								<path
									d="M4.5 6.5h2.1l1.2 9.2h9.4l1.8-6.6H8.1M9.2 19.2a.9.9 0 100-1.8.9.9 0 000 1.8zm7.4 0a.9.9 0 100-1.8.9.9 0 000 1.8z"
									stroke="currentColor"
									stroke-width="1.7"
									stroke-linecap="round"
									stroke-linejoin="round"
								/>
							</svg>
							<span class="hero__buy-label" data-edit="ctaBuy">
								{brandCopy.ctaBuy}
							</span>
							<span class="hero__buy-price">
								{en.hero.priceFrom} ${monthly.price}
							</span>
						</a>
					</div>
					<ul class="hero__features">
						{chips.map((chip) => (
							<li>
								<svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
									{chip.icon === 'esp' && (
										<>
											<circle cx="12" cy="12" r="7.5" stroke="currentColor" stroke-width="1.6" />
											<path
												d="M12 8.5v3.2L14.2 14"
												stroke="currentColor"
												stroke-width="1.6"
												stroke-linecap="round"
											/>
										</>
									)}
									{chip.icon === 'aim' && (
										<path
											d="M12 4.5l1.7 4.8H19l-4 3.1 1.5 4.8L12 14.8 7.5 17.2 9 12.4 5 9.3h5.3L12 4.5z"
											stroke="currentColor"
											stroke-width="1.5"
											stroke-linejoin="round"
										/>
									)}
									{chip.icon === 'radar' && (
										<>
											<circle cx="12" cy="12" r="3" stroke="currentColor" stroke-width="1.6" />
											<circle cx="12" cy="12" r="7.5" stroke="currentColor" stroke-width="1.6" />
											<path
												d="M12 2.8v2.2M12 19v2.2M2.8 12h2.2M19 12h2.2"
												stroke="currentColor"
												stroke-width="1.6"
												stroke-linecap="round"
											/>
										</>
									)}
									{chip.icon === 'updates' && (
										<path
											d="M5 13.2l4.2 4.2L19 7.6"
											stroke="currentColor"
											stroke-width="1.8"
											stroke-linecap="round"
											stroke-linejoin="round"
										/>
									)}
								</svg>
								<span data-edit={chip.key}>{chip.label}</span>
							</li>
						))}
					</ul>
				</div>
			</div>
		</section>
	) : (
		<HeroApp
			client:visible
			locale={locale}
			siteName={siteConfig.name}
			checkoutUrl={siteConfig.checkoutUrl}
			monthlyPrice={monthly.price}
			heroSrc={heroSrc}
			heroSrcSet={heroSrcSet}
			heroSizes={heroSizes}
			heroWidth={heroWidth}
			heroHeight={heroHeight}
			useBrandHero={false}
		/>
	)
}

<style is:global>
	.hero {
		--hero-pad-y: clamp(1rem, 2vw, 1.75rem);
		--hero-stack: clamp(0.5rem, 1.1vw, 0.75rem);
		/*
		 * Match native ~2.5:1 art ratio — less crop = sharper hero on wide screens.
		 */
		--hero-ratio: 2.5 / 1;
		position: relative;
		isolation: isolate;
		display: grid;
		align-items: center;
		overflow: hidden;
		background: var(--bg);
		aspect-ratio: var(--hero-ratio);
		width: 100%;
		height: auto;
		min-height: 0;
		max-height: none;
	}

	.hero__media {
		position: absolute;
		inset: 0;
		z-index: 0;
	}

	.hero__media picture,
	.hero__media img {
		display: block;
		width: 100%;
		height: 100%;
	}

	.hero__media img {
		object-fit: cover;
		object-position: center 42%;
		image-rendering: auto;
		transform: translateZ(0);
		backface-visibility: hidden;
		animation: hero-in 0.9s ease-out both;
	}

	.hero__veil {
		position: absolute;
		inset: 0;
		z-index: 1;
		pointer-events: none;
		background: linear-gradient(
			90deg,
			color-mix(in srgb, var(--bg) 88%, transparent) 0%,
			color-mix(in srgb, var(--bg) 58%, transparent) 32%,
			color-mix(in srgb, var(--bg) 22%, transparent) 56%,
			transparent 72%
		);
	}

	.hero__content {
		position: relative;
		z-index: 2;
		padding-block: var(--hero-pad-y);
		align-self: center;
		/* Sit a touch above true center so title reads mid-image */
		transform: translateY(clamp(-1.5rem, -3vw, -0.35rem));
		display: grid;
		grid-template-columns: minmax(0, 1fr);
		justify-items: start;
		box-sizing: border-box;
	}

	.hero__copy {
		display: flex;
		flex-direction: column;
		align-items: stretch;
		gap: var(--hero-stack);
		width: min(100%, 28rem);
		max-width: 100%;
		min-width: 0;
		margin: 0;
		padding: 0;
		animation: rise 0.65s ease-out both;
	}

	.hero__brand {
		margin: 0;
		font-family: var(--font-sans);
		font-size: clamp(2.1rem, 4.4vw, 3.25rem);
		font-weight: 700;
		line-height: 1.12;
		letter-spacing: -0.03em;
		overflow-wrap: anywhere;
		color: #fff;
		width: 100%;
		text-transform: uppercase;
	}

	.hero__lede {
		margin: 0;
		width: 100%;
		max-width: none;
		font-size: clamp(0.95rem, 1.5vw, 1.05rem);
		line-height: 1.5;
		color: color-mix(in srgb, #fff 74%, var(--ink-muted));
	}

	.hero__actions {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 0.7rem;
		margin: 0;
		width: 100%;
	}

	.hero__buy {
		display: inline-flex;
		align-items: center;
		justify-content: flex-start;
		gap: 0.55rem;
		width: auto;
		max-width: 100%;
		min-height: 3rem;
		padding: 0.55rem 0.55rem 0.55rem 1.05rem;
		border-radius: 999px;
		background: var(--magenta);
		color: #0a1628;
		font-size: 0.9375rem;
		font-weight: 700;
		letter-spacing: -0.015em;
		box-shadow:
			0 1px 0 color-mix(in srgb, #fff 70%, transparent) inset,
			0 10px 28px rgba(0, 0, 0, 0.28);
		transition:
			transform 0.18s ease,
			background 0.18s ease,
			box-shadow 0.18s ease;
	}

	.hero__buy svg {
		width: 1.05rem;
		height: 1.05rem;
		flex-shrink: 0;
		opacity: 0.85;
	}

	.hero__buy-label {
		padding-right: 0.15rem;
	}

	.hero__buy-price {
		display: inline-flex;
		align-items: center;
		min-height: 2rem;
		margin-left: auto;
		padding: 0 0.85rem;
		border-radius: 999px;
		background: color-mix(in srgb, var(--bg) 88%, transparent);
		color: #fff;
		font-size: 0.8125rem;
		font-weight: 700;
		letter-spacing: -0.01em;
	}

	.hero__buy:hover {
		background: var(--accent-hover);
		color: #0a1628;
		transform: translateY(-2px);
		box-shadow:
			0 1px 0 color-mix(in srgb, #fff 40%, transparent) inset,
			0 14px 32px rgba(0, 0, 0, 0.34);
	}

	.hero__buy:hover .hero__buy-price {
		background: var(--bg);
	}

	.hero__buy:active {
		transform: translateY(0);
	}

	.hero__features {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		column-gap: 1rem;
		row-gap: 0.55rem;
		margin: 0;
		padding: 0;
		list-style: none;
		width: 100%;
		max-width: none;
	}

	.hero__features li {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		min-width: 0;
		font-size: 0.8125rem;
		font-weight: 500;
		line-height: 1.3;
		color: color-mix(in srgb, #fff 72%, transparent);
	}

	.hero__features li span {
		min-width: 0;
	}

	.hero__features svg {
		width: 1rem;
		height: 1rem;
		flex-shrink: 0;
		color: var(--magenta-soft);
	}

	@keyframes rise {
		from {
			opacity: 0;
			transform: translateY(10px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes hero-in {
		from {
			opacity: 0.7;
		}
		to {
			opacity: 1;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.hero__copy,
		.hero__media img {
			animation: none;
		}

		.hero__buy,
		.hero__buy:hover {
			transition: none;
			transform: none;
		}

		.hero__content {
			transform: translateY(clamp(-1.5rem, -3vw, -0.35rem));
		}
	}

	/*
	 * Phones / small tablets: stop using the ultra-wide 3.15:1 bar.
	 * That ratio collapses to ~120px tall on a 390px phone and clips the CTA.
	 * Mobile = taller full-bleed panel; height grows with copy.
	 */
	@media (max-width: 900px) {
		.hero {
			aspect-ratio: auto;
			min-height: 0;
			max-height: none;
			height: auto;
			display: block;
			overflow: hidden;
		}

		.hero__media img {
			object-fit: cover;
			object-position: 78% 10%;
		}

		.hero__veil {
			background: linear-gradient(
				180deg,
				rgba(5, 5, 6, 0.28) 0%,
				rgba(5, 5, 6, 0.52) 42%,
				rgba(5, 5, 6, 0.9) 78%,
				rgba(5, 5, 6, 0.96) 100%
			);
		}

		.hero__content {
			transform: none;
			width: 100%;
			min-height: clamp(18.5rem, 56svh, 28rem);
			display: flex;
			align-items: flex-end;
			padding-block: 1.35rem 1.2rem;
			padding-bottom: max(1.2rem, env(safe-area-inset-bottom, 0px));
			box-sizing: border-box;
		}

		.hero__copy {
			width: 100%;
			max-width: 28rem;
			gap: 0.65rem;
		}

		.hero__brand {
			font-size: clamp(1.7rem, 7.5vw, 2.35rem);
			line-height: 1.08;
		}

		.hero__lede {
			display: block;
			overflow: visible;
			-webkit-line-clamp: unset;
			font-size: clamp(0.9rem, 3.6vw, 1rem);
			max-width: 34ch;
			color: color-mix(in srgb, #fff 80%, var(--ink-muted));
		}

		.hero__actions {
			width: 100%;
		}

		.hero__buy {
			width: 100%;
			max-width: 22rem;
			min-height: var(--touch-min, 2.75rem);
			font-size: 0.9375rem;
			justify-content: flex-start;
		}

		.hero__buy-price {
			margin-left: auto;
		}

		.hero__features {
			display: grid;
			grid-template-columns: repeat(2, minmax(0, 1fr));
			column-gap: 0.75rem;
			row-gap: 0.45rem;
			max-width: 22rem;
		}

		.hero__features li {
			font-size: 0.78rem;
		}
	}

	@media (max-width: 480px) {
		.hero__content {
			min-height: clamp(17.5rem, 60svh, 26rem);
			padding-block: 1.2rem 1.1rem;
		}

		.hero__brand {
			font-size: clamp(1.55rem, 8.2vw, 1.95rem);
		}

		.hero__lede {
			max-width: none;
		}

		.hero__buy {
			width: 100%;
			max-width: none;
		}

		.hero__features {
			max-width: none;
		}
	}

	@media (max-width: 360px) {
		.hero__content {
			min-height: clamp(16.5rem, 64svh, 24rem);
		}

		.hero__brand {
			font-size: 1.45rem;
		}

		.hero__features {
			grid-template-columns: 1fr;
			row-gap: 0.35rem;
		}
	}

	@media (max-width: 900px) and (orientation: landscape) and (max-height: 480px) {
		.hero__content {
			min-height: min(70svh, 16.5rem);
			padding-block: 0.85rem 0.8rem;
		}

		.hero__lede {
			display: -webkit-box;
			-webkit-box-orient: vertical;
			-webkit-line-clamp: 2;
			overflow: hidden;
			font-size: 0.85rem;
		}

		.hero__features {
			display: none;
		}

		.hero__buy {
			max-width: 20rem;
		}
	}
</style>
